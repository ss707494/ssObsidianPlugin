// @ts-nocheck
import React, {useEffect, useState} from 'react'
import {getMetaedit} from '../utils/utils'

export const TestBox = (props: any) => {
	// document.querySelector('.mobile-navbar-action .lucide-plus-circle').parentElement.parentElement.parentElement.style.background = '#fff'
	return <div>
		{/*{`${document.querySelector('.mobile-navbar-action .lucide-plus-circle').parentElement.parentElement.outerHTML}`}*/}
	</div>
}
